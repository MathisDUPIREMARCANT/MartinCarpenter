#include "Header.h"
#include <stdlib.h>
#include <stdio.h>
#include <string.h>

void solver(char* Board, Coord posMax, Coord pos, int Direction[4]) {

	if (Direction != NULL) {

		Coord Copy_pos;
		
		for (int i = 0; i < 4; i++) {
			Copy_pos = { pos.x, pos.y };
		}
	}
}